# Class names for palmCounting-model.onnx
# Not supplied with the exam materials. Confirmed via a dummy inference pass
# (ONNX output shape = (1, 25200, 6) -> 4 box coords + 1 objectness + 1 class
# score => single-class model). Named after the sample raster (sawit = oil palm).
names:
  0: sawit
